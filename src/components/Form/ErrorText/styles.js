import React from 'react';
import {
  StyleSheet,
} from "react-native";


const styles = StyleSheet.create({
  errorContainer: {
    paddingVertical: 10,
  },
  errorText: {
    fontSize: 14,
    color: "red",
  },
});

export default styles;
